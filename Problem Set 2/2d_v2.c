#include <stdio.h>

int main(void)
{
    int num;
    char string[1000001];
    scanf("%d", &num);
    for (int i = 0; i < num; i++)
    {
        scanf("%s", &string);
        for
            i
    }
    return 0;
}
